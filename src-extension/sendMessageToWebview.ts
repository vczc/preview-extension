import * as vscode from 'vscode'
import * as path from 'path'
import * as sudo from 'sudo-prompt'

//处理接受数据
export function sendMessageToWebview(pannel: vscode.WebviewPanel) {
  
    // 1. all build pages data
    
}